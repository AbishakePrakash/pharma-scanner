import { Link, Stack, Box, LoadingButton, IconButton, InputAdornment, TextField, Checkbox, FormControlLabel} from '@mui/material';

function App() {
  return (
    <Box component='form'  sx={{ width: '60%' }}>
      App page
    </Box>
  );
}

export default App;
